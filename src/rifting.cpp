#include <iostream>

#include "tectonicPhenomenon.h"
#include "planet.h"

void Rifting::triggerEvent(Planet& planet) const {
    // std::cout << "Rifting event triggered at vertex " << getVertexIndex() << " between plates " << getPlateA() << " and " << getPlateB() << std::endl;
}